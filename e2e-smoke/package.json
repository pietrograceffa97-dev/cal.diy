{
  "name": "cal-diy-smoke",
  "version": "0.0.0",
  "private": true,
  "description": "Standalone production smoke tests. Deliberately outside the yarn workspaces — see README.md.",
  "scripts": {
    "test": "playwright test",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "1.57.0"
  }
}
